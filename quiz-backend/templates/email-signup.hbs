<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: #0d1117;
        font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }

      .wrapper {
        width: 100%;
        max-width: 520px;
        padding: 20px;
        box-sizing: border-box;
      }

      .email-container {
        background-color: #161b22;
        color: #ffffff;
        border-radius: 12px;
        padding: 40px 30px;
        text-align: center;
        box-shadow: 0 10px 35px rgba(0, 0, 0, 0.3);
      }

      .header {
        font-size: 24px;
        font-weight: 700;
        margin-bottom: 16px;
        color: #ffffff;
      }

      .message {
        font-size: 16px;
        margin-bottom: 24px;
        line-height: 1.5;
        color: #ffffff;
      }

      .otp-box {
        font-size: 36px;
        font-weight: bold;
        letter-spacing: 12px;
        background-color: #21262d;
        padding: 16px 0;
        margin: 24px 0;
        border-radius: 10px;
        color: #58a6ff;
      }

      .footer {
        font-size: 13px;
        color: #8b949e;
        margin-top: 30px;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="email-container">
        <div class="header">Welcome to DoubtApp!</div>
        <div class="message">
          Hello {{username}},<br />
          Your email verification code is:
        </div>
        <div class="otp-box">{{verficationCode}}</div>
        <div class="message">
          Please enter this code in the app to verify your email and complete your signup.
        </div>
        <div class="footer">
          If you didn’t request this email, you can safely ignore it.
        </div>
      </div>
    </div>
  </body>
</html>
